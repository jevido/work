// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * WorkbenchService exposes the agent workbench to the frontend.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as agents$0 from "../agents/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as board$0 from "../board/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as changes$0 from "../changes/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as workbench$0 from "../workbench/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * ActivateTab makes a tab the one agents run in, which means running them in
 * the folder this machine bound to it. Refused while a run is in flight.
 */
export function ActivateTab(tabID: string): $CancellablePromise<void> {
    return $Call.ByID(2060135979, tabID);
}

/**
 * AgentProfile returns what one agent's folder says about them: the skills in
 * skills/, their PERSONALITY.md and the line Anton routes them on. The profile
 * view asks for this when it opens, rather than reading it out of the roster,
 * because the roster is re-sent constantly and this is not small.
 */
export function AgentProfile(agentID: string): $CancellablePromise<agents$0.Profile> {
    return $Call.ByID(743926343, agentID);
}

/**
 * Agents returns the team and each member's current state.
 */
export function Agents(): $CancellablePromise<workbench$0.AgentStatus[] | null> {
    return $Call.ByID(294173339);
}

/**
 * ApplyWorkspaceEdits writes orientation and isolation edits into a tab's document and
 * hands back the merged result.
 * 
 * This is the call the outline did not have. Before it, the frontend had its
 * own replica -- its own actor, its own Lamport clock, its own outbox in
 * localStorage -- which made two writers out of one machine, and the outline
 * could not leave the webview it was typed in. Now the edit is described here
 * and everything only this machine may mint is minted once, by the Sync that
 * owns the workspace: the op ID, the actor, the clock.
 * 
 * It returns the whole document rather than nothing, so the caller sees its
 * own edit merged without a second round trip that could race the sync loop.
 * An edit that reached this call is on disk before it returns; reaching the
 * server is the second stage and happens afterwards, or offline, later.
 * 
 * ErrNoTransport comes back on a machine that has joined no workspace, which
 * is the ordinary case and not an error to show: there is no shared document
 * to write to, and the caller keeps its own.
 */
export function ApplyWorkspaceEdits(tabID: string, edits: workbench$0.Edit[] | null): $CancellablePromise<workbench$0.Document> {
    return $Call.ByID(1081182993, tabID, edits);
}

/**
 * BindTabFolder asks the user which project on this machine a tab means,
 * using the platform's own folder picker, and remembers the answer.
 * 
 * The folder is per-machine and never leaves it: the workspace knows the tab,
 * not where anybody keeps their code. A cancelled dialog returns an empty
 * path and no error, exactly as SelectConfigFolder does.
 */
export function BindTabFolder(tabID: string): $CancellablePromise<string> {
    return $Call.ByID(3551732825, tabID);
}

/**
 * Board returns the task board: what Anton has assigned and how far along it is.
 */
export function Board(): $CancellablePromise<board$0.Card[] | null> {
    return $Call.ByID(1742214943);
}

/**
 * Cancel stops a run, including any specialists working inside it.
 */
export function Cancel(runID: string): $CancellablePromise<void> {
    return $Call.ByID(4037848303, runID);
}

/**
 * Changes lists the files the last run touched, with their diffs.
 */
export function Changes(): $CancellablePromise<changes$0.Change[] | null> {
    return $Call.ByID(2944790278);
}

/**
 * Chat asks a question without starting a run, so the input stays usable while
 * specialists are working. Answered by whoever leads the conversation's mode.
 */
export function Chat(conv: workbench$0.Conversation, prompt: string): $CancellablePromise<workbench$0.Task> {
    return $Call.ByID(4294301679, conv, prompt);
}

/**
 * ClearBoard empties the board and puts the tab's plan back on it. What
 * clearing a conversation used to do on the way past, now that a conversation
 * is one of many and the board belongs to none of them.
 */
export function ClearBoard(): $CancellablePromise<void> {
    return $Call.ByID(1302749758);
}

/**
 * ClearConversation starts one transcript over: the agents forget what was said
 * in it, and the next request there opens a fresh session.
 * 
 * One transcript. Every other conversation, and the board, are left alone --
 * see Workbench.ClearConversation, and ClearBoard for the half that moved.
 */
export function ClearConversation(conv: workbench$0.Conversation): $CancellablePromise<void> {
    return $Call.ByID(1659856941, conv);
}

/**
 * CloseTab retires a tab for everyone in the workspace.
 */
export function CloseTab(tabID: string): $CancellablePromise<void> {
    return $Call.ByID(1990543030, tabID);
}

/**
 * CreateLocalWorkspace makes a workspace that lives only on this machine and
 * opens it. Nothing is sent anywhere and no server is involved, which is why
 * it takes neither an address nor a token.
 * 
 * This is what setup calls. Work has always run perfectly well without a
 * server, and the version of that where you also get tabs, a shared outline
 * and a board is this.
 */
export function CreateLocalWorkspace(name: string): $CancellablePromise<workbench$0.WorkspaceView | null> {
    return $Call.ByID(3141267105, name);
}

/**
 * CreateWorkspace makes a workspace on a server and joins it.
 * 
 * signupToken is the server's own signup token, not a workspace key: there is
 * no workspace to authenticate against yet. It is empty against a server that
 * does not gate creation, which is the default.
 */
export function CreateWorkspace(serverURL: string, signupToken: string, name: string): $CancellablePromise<workbench$0.WorkspaceView | null> {
    return $Call.ByID(3934875234, serverURL, signupToken, name);
}

/**
 * DocumentationSite reads the configured site. Read on every call rather than
 * cached: the config file is edited by hand, and a cached answer would mean
 * restarting the app after fixing a typo in it.
 */
export function DocumentationSite(): $CancellablePromise<$models.DocumentationSite> {
    return $Call.ByID(1281190174);
}

/**
 * ForgetWorkspaceKeys takes a workspace's keys off this machine. The joined
 * workspace is refused: leaving it is LeaveWorkspace.
 */
export function ForgetWorkspaceKeys(id: string): $CancellablePromise<void> {
    return $Call.ByID(705857877, id);
}

/**
 * GetConfigPath returns the folder Work loads agents from, or an empty string
 * if the user has not picked one yet. An empty result is the first-run signal.
 */
export function GetConfigPath(): $CancellablePromise<string> {
    return $Call.ByID(1817986202);
}

/**
 * JoinWorkspace joins an existing workspace with its write key. The key
 * identifies the workspace, so there is no ID to supply.
 */
export function JoinWorkspace(serverURL: string, writeKey: string): $CancellablePromise<workbench$0.WorkspaceView | null> {
    return $Call.ByID(13941062, serverURL, writeKey);
}

/**
 * KnownWorkspaces lists every workspace this machine holds keys for, the
 * joined one first.
 * 
 * The keys are on it. They are handed out on purpose and the server keeps only
 * hashes, so the copy here is the only one anybody has -- which is the whole
 * reason it is worth keeping and worth showing. It is asked for by name, like
 * WorkspaceKeys, rather than riding along on a status payload.
 */
export function KnownWorkspaces(): $CancellablePromise<workbench$0.KnownWorkspace[] | null> {
    return $Call.ByID(2084538778);
}

/**
 * LeaveWorkspace stops syncing and returns Work to running purely locally.
 * Ops that never reached the server are kept, not discarded.
 */
export function LeaveWorkspace(): $CancellablePromise<void> {
    return $Call.ByID(4131119665);
}

/**
 * MintKey issues another key for the joined workspace: "read" for a link to
 * send somebody, "write" for another machine to join with.
 * 
 * There is no call that reads a key back, because the server keeps only hashes
 * of them. Asking for one is asking for a new one, and the keys already in use
 * keep working.
 */
export function MintKey(access: string): $CancellablePromise<string> {
    return $Call.ByID(221255780, access);
}

/**
 * MintKeyForWorkspace issues another key for any workspace this machine has
 * the write key for, joined or not. It is how a read key is got for a
 * workspace whose read key was never written down.
 */
export function MintKeyForWorkspace(id: string, access: string): $CancellablePromise<string> {
    return $Call.ByID(3192207920, id, access);
}

/**
 * NewTab creates a workspace tab. It has no folder yet -- see BindTabFolder.
 */
export function NewTab(name: string): $CancellablePromise<workbench$0.TabView> {
    return $Call.ByID(2885008436, name);
}

/**
 * NextTask is the first task on the active tab's plan that is still to do.
 * 
 * The whole task rather than its id: the caller wants to show it, and a second
 * round trip to ask what an id means is a frame of empty button.
 * 
 * The second return is false when there is nothing to offer -- an empty plan,
 * no workspace, a tab this machine has bound to no folder, or a plan where
 * everything is finished. None of those is an error.
 */
export function NextTask(): $CancellablePromise<[workbench$0.PlanTask, boolean]> {
    return $Call.ByID(3928550185);
}

/**
 * Permissions returns the current mode and everything needed to describe it.
 */
export function Permissions(): $CancellablePromise<$models.Permissions> {
    return $Call.ByID(1208458807);
}

/**
 * PublishPage writes one page to the documentation site.
 * 
 * Called from documentation mode, by a person, after reading what the agent
 * wrote. The app publishes rather than the model: the MCP server the model
 * talks to has no network in it and may never have any, so the one thing that
 * leaves this machine leaves it because somebody pressed a button.
 */
export function PublishPage(slug: string, title: string, description: string, markdown: string): $CancellablePromise<$models.PublishResult> {
    return $Call.ByID(1030555959, slug, title, description, markdown);
}

/**
 * ReloadAgents rescans the config folder and rebuilds the team, so an agent
 * folder added outside Work shows up without a restart.
 */
export function ReloadAgents(): $CancellablePromise<workbench$0.AgentStatus[] | null> {
    return $Call.ByID(2820324798);
}

/**
 * ResetToServer discards this machine's unsent work and reads the workspace
 * back from the server.
 * 
 * Destructive and not undoable: the outbox is deleted rather than parked. The
 * frontend asks before calling it.
 */
export function ResetToServer(): $CancellablePromise<void> {
    return $Call.ByID(1171039842);
}

/**
 * Restructure asks Claude to propose changes to a tab's outline or plan.
 * 
 * Returns when the run has started, not when it has answered. The proposal
 * itself never comes back through this call: Claude answers by calling a tool,
 * the tool call goes past on the stream the console is already reading, and the
 * review panel picks it up there. Nothing is applied until somebody presses
 * Apply.
 * 
 * mode is "orientation" or "isolation". They ask different questions of the same
 * document -- reorganise, or break into tasks -- and are given different state
 * to answer from.
 */
export function Restructure(tabID: string, mode: string, request: string): $CancellablePromise<workbench$0.Task> {
    return $Call.ByID(2296734873, tabID, mode, request);
}

/**
 * Revert undoes one of those file changes, restoring the content the run
 * started with rather than the last commit.
 */
export function Revert(path: string): $CancellablePromise<void> {
    return $Call.ByID(1452874559, path);
}

/**
 * ReviewProposalsFirst reports whether Claude's proposed restructurings wait in
 * the review panel instead of applying themselves.
 * 
 * Off by default. The panel used to be the only approval gate this app had, and
 * it was worth a click on every proposal for that reason; a change that lands
 * and can be undone in one press does not need one in front of it.
 */
export function ReviewProposalsFirst(): $CancellablePromise<boolean> {
    return $Call.ByID(123238850);
}

/**
 * SaveNow sends what a held workspace has been keeping back, and answers with
 * where sync stands afterwards.
 * 
 * The status comes back rather than nothing, for the reason the proposal
 * toggle gives: the control draws from what the backend holds, rather than
 * from an assumption that its own click won.
 */
export function SaveNow(): $CancellablePromise<workbench$0.Status> {
    return $Call.ByID(1475226236);
}

/**
 * SelectConfigFolder asks the user for a config folder with the platform's own
 * folder picker, then loads the team from it and remembers the choice.
 * 
 * A cancelled dialog returns an empty path and no error: the user declining is
 * not a failure, and the caller can tell the two apart by the empty string.
 */
export function SelectConfigFolder(): $CancellablePromise<string> {
    return $Call.ByID(1893891007);
}

/**
 * SetHoldPush decides whether this workspace pushes as it goes or waits for
 * Save. Per workspace, written down, and off by default.
 */
export function SetHoldPush(hold: boolean): $CancellablePromise<workbench$0.Status> {
    return $Call.ByID(3384461926, hold);
}

/**
 * SetPermissionMode changes what agents may do, for this session and the next.
 * 
 * It answers with the whole setting rather than nothing, so the toggle draws
 * from what the backend actually holds instead of assuming its own click won.
 * An unknown mode is an error and changes nothing.
 */
export function SetPermissionMode(mode: string): $CancellablePromise<$models.Permissions> {
    return $Call.ByID(1553242683, mode);
}

/**
 * SetReviewProposalsFirst changes it, and answers with what the backend now
 * holds rather than nothing -- so the control draws from the setting instead of
 * assuming its own click won.
 */
export function SetReviewProposalsFirst(first: boolean): $CancellablePromise<boolean> {
    return $Call.ByID(3984145332, first);
}

/**
 * SetTabFolder points a tab at a folder by path, for somebody who would rather
 * type or paste one than walk a picker.
 * 
 * The same call the picker ends in, with the same validation behind it: a path
 * that does not exist, or names a file, is refused by Workbench.BindTab and the
 * tab keeps the folder it had. A leading ~ is expanded here because a path
 * typed by a person is a path typed the way people write them, and nothing
 * further down knows what a home directory is.
 */
export function SetTabFolder(tabID: string, path: string): $CancellablePromise<string> {
    return $Call.ByID(319491438, tabID, path);
}

/**
 * StartNextTask runs the next task on the active tab's plan.
 * 
 * The order isolation holds is the order implementation takes: this is what makes that
 * true rather than decorative. Refused, with a reason that distinguishes them,
 * when there is no workspace, no folder for this tab on this machine, or
 * nothing left to do.
 */
export function StartNextTask(): $CancellablePromise<workbench$0.Task> {
    return $Call.ByID(3067328503);
}

/**
 * Submit starts a run in a conversation. An empty agentID gives the task to the
 * coordinator, who decides whether to answer it himself or split it between
 * specialists.
 * 
 * The conversation is not validated. An unknown tab id is a transcript nobody
 * will look at rather than an error, and refusing one would break the machine
 * that has joined no workspace and whose tab id is therefore empty.
 */
export function Submit(conv: workbench$0.Conversation, agentID: string, prompt: string): $CancellablePromise<workbench$0.Task> {
    return $Call.ByID(1590098313, conv, agentID, prompt);
}

/**
 * SyncNow pushes and pulls immediately instead of waiting for the next poll.
 * It is the retry behind an offline indicator, and the way back from a
 * rejected key once it has been replaced.
 */
export function SyncNow(): $CancellablePromise<void> {
    return $Call.ByID(1811848030);
}

/**
 * SyncStatus is where the second stage stands: online, syncing, offline or
 * rejected, how many ops are waiting, and how far behind the server this
 * machine is. Joined is false when no workspace has been joined.
 * 
 * The frontend does not need to poll this -- workspace:sync carries the same
 * value whenever it changes. This is for the first paint.
 */
export function SyncStatus(): $CancellablePromise<workbench$0.Status> {
    return $Call.ByID(1820193232);
}

/**
 * Workspace returns the joined workspace, or null on a machine that has not
 * joined one. Null is the ordinary case.
 * 
 * The keys are not on it. Asking for those is WorkspaceKeys, so a credential
 * never rides along on a payload the frontend fetches as a matter of course.
 */
export function Workspace(): $CancellablePromise<workbench$0.WorkspaceView | null> {
    return $Call.ByID(4150022866);
}

/**
 * WorkspaceCards is one tab's cards as the whole workspace sees them. Board
 * remains this machine's own board; this is everyone's.
 */
export function WorkspaceCards(tabID: string): $CancellablePromise<board$0.Card[] | null> {
    return $Call.ByID(2518714625, tabID);
}

/**
 * WorkspaceDocument is the merged workspace: every tab, and every card from
 * every machine that has joined.
 * 
 * Fetched rather than pushed, because it changes when someone edits something
 * and not on the poll. Call it when the cursor on a workspace:sync event
 * moves.
 */
export function WorkspaceDocument(): $CancellablePromise<workbench$0.Document> {
    return $Call.ByID(2145133807);
}

/**
 * WorkspaceKeys returns both of the workspace's keys.
 * 
 * Both, always, and the same two every time: a machine that joined with a
 * write key and so has no read key gets one minted and kept on the first ask,
 * so opening this panel twice shows one pair rather than issuing a credential
 * each time. See Workbench.EnsureKeys.
 */
export function WorkspaceKeys(): $CancellablePromise<workbench$0.Keys> {
    return $Call.ByID(2941481374);
}

/**
 * Workspaces reports whether this build can talk to a workspace server at
 * all. The UI hides the workspace controls when it cannot, rather than
 * offering buttons whose only outcome is an error.
 */
export function Workspaces(): $CancellablePromise<boolean> {
    return $Call.ByID(123081587);
}
